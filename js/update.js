function update()
{
  for(a = 0; a<zoo.length; a++){
    /*var A = zoo[a];
    var t = A.get('x');
    A.set('x',t+1);
    t = A.get('y');
    A.set('y',t+1);*/
    zoo[a].updatePosition();
  }










}
